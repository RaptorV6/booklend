# Disable directory listing
Options -Indexes

# Enable rewrite engine
RewriteEngine On

# Redirect to public folder (but keep URL clean)
RewriteCond %{REQUEST_URI} !^/booklend/public/
RewriteRule ^(.*)$ public/$1 [L]
